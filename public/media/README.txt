Put a real storefront or bay photo here as:

  hero.jpg

or:

  hero.webp

Then `npm run build` will swap the hero automatically (see `lib/media.ts`).

Recommended: horizontal 2400×1600 px+, trimmed for subject + dark overlay friendly.
